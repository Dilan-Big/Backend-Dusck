info:
  name: Crear Usuario
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/users
  body:
    type: json
    data: |-
      {
        "name": "David Velez",
        "nickname": "David123",
        "email": "DavidVelez@example.com",
        "password": "Password12"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
