info:
  name: Registro publco de usuarios
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/auth/register
  body:
    type: json
    data: |-
      {
        "name": "Sebastian Garzon",
        "nickname": "Sebas123",
        "email": "SebasGarzon@example.com",
        "password": "Password12*!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
