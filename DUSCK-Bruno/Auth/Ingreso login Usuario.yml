info:
  name: Ingreso login Usuario
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "DanielSanchez@example.com",
        "password": "Password1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
